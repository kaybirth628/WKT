---
description: WKT 生产安全：云端=正式数据，禁止覆盖/清空云端业务数据；仅3个bat；代码只部署不同步本地库上云。
alwaysApply: true
---

# WKT 生产环境安全（强制）

## 背景

云端 = **生产业务数据**。本地开发，**只上传程序代码**，不上传本地数据库覆盖云端。

## 唯一运维 bat

1. `一键启动网页.bat`
2. `一键推送云端和GitHub.bat`（代码 + GitHub，**不**覆盖云端 data 业务库）
3. `一键下载云端数据覆盖本地.bat`（云→本地，本地先备份）

## 禁止生成/建议

- TRUNCATE/DROP/清空表、无条件批量 UPDATE、全量覆盖云端 data
- 同步本地 DB 至云端的代码
- 硬编码云端 DB 连接
- `-FullData` / 全量 master 同步（脚本已禁用）

## 数据库

- 仅 **增量迁移**；修复脚本标注【严禁生产一键执行】
- 删除/批量改：生产环境二次确认 + 审计日志

## Agent

触碰上述规则 → **先告警**，不得直接输出危险可执行代码。

全文：[`docs/change/PRODUCTION-SAFETY.md`](../../docs/change/PRODUCTION-SAFETY.md)
